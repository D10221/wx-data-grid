{
  "name": "wx-data-grid",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "npm install && tsd install",
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -fr ./built && rm -fr ./dist",
    "build": "tsc -p src",
    "rebuild": "npm run clean && npm run build",
    "bundle-app": "webpack",
    "re-bundle-app": "npm run rebuild; webpack"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "lodash": "^4.11.1",
    "moment": "^2.13.0",
    "rx": "^4.1.0",
    "webrx": "^1.5.0",
    "whatwg-fetch": "^0.11.0"
  },
  "devDependencies": {
    "material-design-lite-oob": "git+https://git@github.com/D10221/material-design-lite-oob.git"
  }
}
